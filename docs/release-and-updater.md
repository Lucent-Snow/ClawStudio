# Updater Notes

当前仓库已经切到 `ClawStudio` 命名，但 updater 公钥和发布仓库仍需在正式发布前重新生成并替换。

## 检查项

- `productName` 为 `ClawStudio`
- `identifier` 为 `com.lucentsnow.clawstudio`
- Release endpoint 指向 `ClawStudio`
- 使用新的 updater signing key
